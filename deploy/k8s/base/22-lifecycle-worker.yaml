apiVersion: apps/v1
kind: Deployment
metadata:
  name: rpa-lifecycle-worker
  namespace: rpa-system
  labels:
    app.kubernetes.io/name: rpa-lifecycle-worker
    app.kubernetes.io/component: lifecycle-worker
    app.kubernetes.io/part-of: rpa-enterprise
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: rpa-lifecycle-worker
      app.kubernetes.io/component: lifecycle-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: rpa-lifecycle-worker
        app.kubernetes.io/component: lifecycle-worker
        app.kubernetes.io/part-of: rpa-enterprise
    spec:
      serviceAccountName: rpa-lifecycle-worker
      automountServiceAccountToken: false
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: rpa-lifecycle-worker
              app.kubernetes.io/component: lifecycle-worker
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: rpa-lifecycle-worker
                    app.kubernetes.io/component: lifecycle-worker
                topologyKey: kubernetes.io/hostname
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: lifecycle-worker
          image: ghcr.io/example/rpa-runtime:replace-me
          imagePullPolicy: IfNotPresent
          ports:
            - name: health
              containerPort: 8081
          envFrom:
            - configMapRef:
                name: rpa-runtime-config
          env:
            - name: RUN_MODE
              value: lifecycle-worker
            - name: PGUSER
              value: rpa_app
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: rpa-runtime-secrets
                  key: RPA_APP_DB_PASSWORD
            - name: ARTIFACT_LIFECYCLE_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: rpa-runtime-secrets
                  key: ARTIFACT_LIFECYCLE_DATABASE_URL
            - name: VAULT_ARTIFACT_LIFECYCLE_ROLE_ID
              valueFrom:
                secretKeyRef:
                  name: rpa-runtime-secrets
                  key: VAULT_ARTIFACT_LIFECYCLE_ROLE_ID
            - name: VAULT_ARTIFACT_LIFECYCLE_SECRET_ID
              valueFrom:
                secretKeyRef:
                  name: rpa-runtime-secrets
                  key: VAULT_ARTIFACT_LIFECYCLE_SECRET_ID
            - name: VAULT_ADDR
              valueFrom:
                secretKeyRef:
                  name: rpa-runtime-secrets
                  key: VAULT_ADDR
            - name: VAULT_MOUNT
              valueFrom:
                secretKeyRef:
                  name: rpa-runtime-secrets
                  key: VAULT_MOUNT
          readinessProbe:
            httpGet:
              path: /readyz
              port: health
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /livez
              port: health
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 2Gi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: ["ALL"]
