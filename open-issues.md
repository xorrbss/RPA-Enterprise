# OPEN ISSUES 레지스터

> `review-qa-admin-prompt.md` 미션의 단일 추적 레지스터. **이슈를 숨기지 않는다.** 발견은 전부 등록하고, 미해결은
> 닫지 않고 명시한다. resolution: **[확정]**(수정 후 CI-green 검증 또는 근거 있는 명시적 수용) · **OPEN**(미해결,
> confidence=confirmed/suspected) · **BLOCK**(외부 사실/미결정 계약 — Required decision로 추적). confidence:
> **confirmed**(증거로 증명) · **suspected**([추정]) · **unknown**([모름]).
> **"완료" = 전 항목 resolution=[확정]** + origin/main Contract Gates green(conclusion=success 재확인).
> OPEN/[추정]/[모름]/BLOCK이 하나라도 남으면 **"사람 검토 대기(NEEDS HUMAN REVIEW)"**. 레지스터는 추가만(append).
>
> 기준: origin/main `212e18cc`. 출처: 6차원 적대적 리뷰 워크플로우(wf_e867d618, 12 findings 전부 refute 통과 real=true) + 직접 스팟리뷰.

## 상태 요약 (2026-06-16, resume3)
- 총 **31** 이슈(원 18 + **재리뷰 13**, RQ-019–031). resolution: **[확정] 23** · **OPEN 6**(RQ-022·023·027·028·029·031; **RQ-025·030 [확정]**) · **BLOCK 1(RQ-016)** · **부분 1(RQ-002)**.
- **현재 "사람 검토 대기(NEEDS HUMAN REVIEW)"** — ①**재리뷰 OPEN 8건**(P1 3건=RQ-019/020/021 + RQ-024 **[확정](PR#67)**; 잔여는 P2/P3 console/codegen/gateway/test + RQ-031) ②**codex 미구현 feature 2건**(RQ-016 suspend/challenge 증분, RQ-002 잔존 loop/fallback) 남음. 완료 아님. **외부 인프라 per-issue BLOCK 없음**.
- **분류 정정(resume3-2, 이중계상 제거)**: RQ-010·011을 "부분(외부 egress BLOCK)"→**[확정]**으로 정정. 사유: 둘의 **repo 산출물**(라우트+메커니즘+테스트+CI-green)은 release-open-checklist의 **"Repo-controlled rows"**(repo 코드+테스트+게이트로 닫힘) 기준 완결이다. "실 분산 object-store(S3)/sink egress 바인딩"은 **이미 제품 전역 deploy-time row**(release-open-checklist L43–52·L59–61 "Hybrid"/"Deploy-time provisioning"; release-decision #13/D6-2/D8-A1)로 **별도 추적** 중이며, 이는 object-store·sink·outbox-bus·SecretStore 등 **모든 외부 port에 균일하게 적용되는 배포 경계**다 — 특정 RQ의 블로커가 아니다. 이를 per-issue BLOCK으로 다시 센 것은 **deploy 경계의 이중계상**이었다. (원 finding="라우트 미구현→capability 도달 불가"는 in-repo로 완전 해소.)
- [확정] 16: RQ-001·003·004·008·009·018(이전) + **RQ-005·006·007·012·013·014·015**(resume2) + **RQ-017**(resume2) + **RQ-010·011**(resume3 — 라우트 빌드; 실 바인딩=제품 전역 deploy row).
- **RQ-002 부분(BLOCK 해소)**: codex가 resume2에서 "node 투영 계약 미정 + codex 소유 활성 증분"으로 무접촉 예약했으나, **운영자가 투영 규약(옵션2=extract `{rows:[...]}` 봉투)을 결정·진행 지시**해 충돌 해소(운영자 권위). 구현(opus, README v2.16): params.*+node.status(PR#57) + **row_count(`output.rows.length`)/extracted_ref(`artifacts[0]`)** + ir-expression §2 명시 → **extract 데이터 분기 가능**. 잔존은 **feature-gated**(계약 미정 아님): `tier`=fallback_chain·`cursor.*`/`loop.until`=loop 미구현 → 해당 feature 구현 시 동일 패턴. ⚠codex 진행 중인 RQ-002 잔존 설계가 있으면 이 결정과 정합 필요(운영자 조율).
- **잔존 2건(닫지 않음 — 둘 다 codex 미구현 feature, 외부 인프라 아님)**:
  - **RQ-016 (BLOCK)** — `human_task.created` producer: producer는 human_tasks INSERT와 분리 불가, INSERT는 challenge/exception **suspend 경로에서만** 발생. 그 경로(`ExecutorChallengeSuspensionPort`)가 미구현 = **codex D3 executor 증분 대기**(R4→R11+resume-token+@challenge/@human_task 핸들러 I/O+계약-적재 필드). 더미 발행은 run 반-상태 좌초(편법 금지); 음성 테스트가 fail-honest 가드. **내가 그 feature를 빌드하면 codex 활성 도메인과 충돌·중복**.
  - **RQ-002 (부분)** — on[] 잔존: params.*+node.{status,row_count,extracted_ref}는 [확정](PR#57/#60); `tier`·`cursor.*`/`loop.until`은 **loop/fallback feature 미구현 의존**(인터프리터가 loop/fallback_chain을 "후속 단계"로 명시 연기 = **codex D3 증분**). 계약 미정 아님(같은 투영 패턴 적용 가능) — feature 빌드가 codex 도메인이라 내가 닫지 않음.
  - (RQ-010·011은 위 [확정] 정정으로 이 목록에서 제외 — 실 object-store/sink 바인딩은 제품 전역 deploy-time row이지 RQ별 블로커 아님.)

## 재리뷰 추가분 (2026-06-16, resume3.5 — 적대적 재스캔 wf_c10fd1d9)
- **신규 12건 등록(RQ-019–030)** — 원 리뷰 베이스라인 `212e18cc` 이후 머지된 ~1240줄 델타(특히 갓 [확정]된 RQ-010/011 라우트·RQ-003 redaction·신규 site-resolution.ts)를 8차원 적대 리뷰 → 결함당 3 회의적 검증(reproduce/contract/impact 렌즈, 과반 refute 폐기). **16 findings 중 12 생존**(4 false-positive 폐기). 전부 confirmed.
- **P1 3건(신규, 보안)**: **RQ-019** artifact.read 라우트 §10 audit-boundary append 누락(fail-open, RQ-010 [확정] 불완전) · **RQ-020** Gateway injection detector 한국어 미탐지(fail-open, RQ-003 [확정] 불완전) · **RQ-021** originOf opaque-scheme(file:/javascript:/data:) 절대URL 수용(fail-open). 셋 다 직접 스팟리뷰로 코드·계약 재확인.
- **P2 6건**: RQ-022(artifact object 부재→500) · RQ-023(hidden_instruction 신호 미구현) · RQ-024(opaque null-origin 오매칭) · RQ-025(openapi getArtifact 404 drift) · RQ-026(badges HumanTask open=RED 충돌) · RQ-027(Gateway 다중정책 dead-end). **P3 3건**: RQ-028(sink_dlq.replay 인가 미검증) · RQ-029(repair redaction 우회) · RQ-030(openapi Artifact 스키마 drift).
- 결과: 이 12건은 모두 **OPEN(confirmed)** — 아직 미수정. in-repo 수정 가능(RQ-019·022·028=control-plane 내 도메인; RQ-025·030=codegen 재생성; RQ-020·023·029=gateway; RQ-026·027=console; RQ-021·024=site-resolution codex 도메인 조율 필요). 우선순위=P1 3건. **여전히 NEEDS HUMAN REVIEW.**

## 레지스터

| ID | dim | 제목 | sev | confidence | resolution | 증거 | 수정 / Required decision |
|---|---|---|---|---|---|---|---|
| RQ-001 | tests-CI | `test:executor`(D3 dry-run) CDP 기동 레이스 — `cdp-session.ts` 단일 `sh.init()` 무재시도 → `ECONNREFUSED` 간헐 실패가 app-runtime 게이트 전체를 비결정적으로 깸(무관 PR도 차단) | P1 | confirmed | **[확정]** | `app/src/executor/cdp-session.ts`; `contract-gates.yml:298-299` | **수정 완료**: `createStagehandSession`에 bounded retry/backoff(ECONNREFUSED/ECONNRESET=retryable, 200·400·800·1600ms, 5회) + 부분기동 정리, 소진 시 `CdpDisconnectedError`(CDP_DISCONNECTED, 원텍스트 미노출), 비-연결거부는 즉시 전파. DI(attemptInit)로 단위검증: `app/test/cdp-session.unit.ts` 4/4(재시도-성공·소진-분류·즉시전파). |
| RQ-002 | correctness | 인터프리터가 on[] scope에 `{flags}`만 전달 → 계약상 허용된 params/node/cursor 참조가 항상 `IREL_RUNTIME_MISSING` throw → 해당 시나리오 실행 불가 | P1 | confirmed | **부분** | `ir-interpreter.ts`(PR#57+후속); `ir-expression.md §2`; `stagehand-dom-executor.ts`; `README v2.14/v2.16` | **on[] 스코프 배선 + node 투영 계약 확정·구현.** PR#57: params.* + node.<id>.status. 후속(README v2.16): **미정이던 StepResult→node 표준출력 투영 규약을 확정**(운영자 결정 — extract 출력 `{rows:[...]}` 봉투; `row_count=output.rows.length`, `extracted_ref=artifacts[0]`, verify min_rows 동일 규약, ir-expression §2 명시) + 구현 → **extract 데이터로 분기 가능**. 검증 interpreter-scope.unit 8(row_count 값-분기·extracted_ref·비-extract 미투영 loud) + interpreter-llm(실 Chrome extract→row_count→분기). **잔존(feature-gated, 계약 미정 아님)**: `tier`(fallback_chain 미구현)·`cursor.*`/`loop.until`(loop 미구현) — 해당 feature 구현 시 동일 패턴 투영. 미투영 참조는 IREL_RUNTIME_MISSING(loud). |
| RQ-003 | security | LLM Gateway에 redaction/prompt-injection 경계 미배선 — `security-contracts §4`는 adapter 전 step2 Gateway-owned redaction+injection 강제(LLMMessage user=RedactedString)인데 `LlmGateway.call`에 단계 없음; 참조 impl(`redaction-boundary.ts`) 미사용 | P1 | confirmed | **[확정]** | `llm-gateway.ts`(redactRequest); `gateway/redaction-boundary.ts`; `security-contracts.md §4` | **수정 완료(보안 enforcement)**: LlmGatewayDeps에 `redactionBoundary: GatewayRedactionBoundary`(required, fail-closed) 추가, call()이 adapter 전 §4 step2로 user 메시지를 `redactForGateway` 통과(비밀/PII 마스킹→RedactedContentBlock[], injection→`PROMPT_INJECTION_DETECTED` 차단). DeterministicGatewayRedactionBoundary 배선(단위 3 case: injection 차단·adapter 미호출·secret 마스킹). **잔여(후속, 비-보안)**: ①executor buildRequest `as unknown` 캐스트 제거(LLMRequest raw-vs-redacted 타입 정리 — gateway 재-redaction으로 런타임 안전) ②networkPolicy 도메인 allowlist injection 체크(networkPolicy 스레딩 필요; instruction-override/credential-exfil 패턴은 적용됨). |
| RQ-004 | contract-fidelity | R5 `@human_task` 전이가 `humanTaskKind="exception"` 하드코딩 → approval/validation kind 유실 → RBAC 오라우팅(approval 태스크를 reviewer가 resolve 가능). 근본: `RunEvent.human_task_required`에 kind 필드 부재(R4 challengeKind와 비대칭) | P1 | confirmed | **[확정]** | `state-machine-types.ts`(event); `codegen/transitions.ts`(R5); `transitions.fixtures.ts`; `state-machine.md` R5 | **수정 완료(계약+codegen)**: `RunEvent.human_task_required`에 `humanTaskKind?: approval\|validation\|exception` 추가(reserved-handlers @human_task input 출처) → R5가 `ev.humanTaskKind ?? "exception"`로 전파(R4 challengeKind 패턴과 동일, 하드코딩 제거) → state-machine.md R5에 kind 규칙 문서화. fixture 87/0(R4 captcha/mfa·R5 exception/approval/validation kind 단언, expectHumanTaskKind 추가). codegen test green. (R5 런타임 배선=후속) |
| RQ-005 | console-fidelity | openGate가 D8-A5(정적 contract-doc 뷰 결정)에도 Placeholder | P1 | confirmed | **[확정]** | `web/src/views/OpenGate.tsx`(신규); `App.tsx`; PR#52 | **수정 완료(PR#52 머지·main green)**: openGate를 정적 contract-doc 뷰로 구현 — 목업 3개 contract-table(Product-open gate map·RBAC action gate·API client contract) React 이식, 로컬 const 데이터·fetch 없음(D8-A5). smoke 단언(gate map 렌더·Placeholder 미노출). web test 47 green. |
| RQ-006 | admin/console | admin gateway-policy 편집 UI 부재 — PUT 백엔드(A2)+RBAC 존재하나 React는 read-only(ApiClient에 updateGatewayPolicy 없음, GatewayPolicy 타입에 version 없음, permissions admin에 gateway_policy.edit 없음) | P1 | confirmed | **[확정]** | `reads.ts`(GET ETag); `client.ts`/`types.ts`/`permissions.ts`/`Gateway.tsx`; `api-gateway.int.ts`; PR#51 | **수정 완료(PR#51 머지·main green)**: 백엔드 GET이 ETag=version 노출(api-surface §145, body 불변) + 프론트 updateGatewayPolicy(PUT If-Match+Idempotency-Key)·GatewayPolicy.version·permissions admin gateway_policy.edit·admin-gated 편집 폼(POLICY_VERSION_CONFLICT/LLM_CAPABILITY_MISMATCH/403 한국어 표면화). api-gateway.int 12/12(GET ETag 포함), web 49 green. |
| RQ-007 | tests-CI | `SitePageStateResolver.resolvePageState`(dev/run-loop 프로덕션 경로)가 `site-page-state.int.ts`로만 검증되는데 그 테스트가 **어느 CI 체인에도 없음**(test:site-resolver 별칭만). CI는 config 파서 unit만 커버 → 런타임 flag 산출 무검증 | P1 | confirmed | **[확정]** | `app/package.json` test:executor; `contract-gates.yml:298`; PR#49 | **수정 완료(PR#49 머지·main green)**: site-page-state.int(Chrome만·DB 불요)를 test:executor 체인에 편입 → app-runtime 'Run D3 executor dry-run tests' 단계가 CHROME_PATH로 실행(무-yml-편집). 로컬 test:executor 3 스위트 green(SitePageStateResolver 8 PASS). CDP=RQ-001 하든. |
| RQ-008 | correctness | IR에 on[] 있으나 compiled_ast에 없으면 빈 branches → `IR_NO_BRANCH_MATCHED`로 표면화(계약상 value 무매칭 전용 코드를 cache drift에 오용) | P2 | confirmed | **[확정]** | `ir-translate.ts`; `ir-static-validation.md §1` | **수정 완료**: `compiledScenarioFrom`이 IR on[] 개수 ≠ compiled_ast on[] 개수(부재 포함)면 `IR_SCHEMA_INVALID`(드리프트) throw — 빈 branches로 떨어뜨리지 않음. 단위검증 `app/test/ir-translate.unit.ts` 5/5(정상·부재드리프트·개수불일치·next/terminal·미지원흐름). |
| RQ-009 | correctness | null 수치 비교 피연산자에서 evaluator가 throw — `ir-expression §3`(null→false 단락)와 `§5`(throw) 충돌, 코드는 §5/no-silent-false 선택 | P2 | confirmed | **[확정]** | `ir-expression.md §3`(개정); `irel-compile.ts:677-685`; README v2.12 | **수정 완료(계약 교정)**: 검증된 내부 모순 — ir-expression.md §3을 코드/§5/no-silent-false에 맞춰 개정(null/부재 수치 피연산자→IREL_RUNTIME_MISSING, false 단락 아님; == null/!= null만 null 명시). 코드 변경 없음(이미 fail-loud). README v2.12 패치로그 기록. 기존 fixture가 fail-loud 검증. |
| RQ-010 | contract-fidelity / role-QA | GET /v1/artifacts/{id} 미구현(api-surface §5/openapi 선언, artifact.read RBAC 존재하나 라우트 없음). 전 역할 artifact 조회 capability 도달 불가 | P2 | confirmed | **[확정]** | `reads.ts`(GET artifacts); `server.ts`(ArtifactObjectReader); `pg-gateway-artifact-sink.ts`(ObjectStore.get); `api-artifacts.int.ts`; `api-surface §5`(v1 404 amend); `release-decisions D8-A1`(Built); PR#63 | **수정 완료(라우트 in-repo·CI green).** GET /v1/artifacts/{id} 빌드: artifact.read RBAC + `artifacts_visible_isolation` RLS를 redaction 게이트로(redacted/not_required·미삭제·비격리만 가시) → pending/failed/quarantined/deleted/cross-tenant ⇒ `RESOURCE_NOT_FOUND`(404, D8-A1, **BYPASSRLS 미사용**, 409 미노출). 200 본문 = injected `ArtifactObjectReader`(ObjectStore.get; FsObjectStore in-repo/CI). **운영자 capability 도달 = 원 finding 해소.** 검증 api-artifacts.int 12. **실 분산 object-store(S3) 바인딩은 RQ별 블로커 아니라 제품 전역 deploy-time row**(release-open-checklist L51–52/L59–61 "Hybrid artifact lifecycle"; object-store·sink·secrets 균일한 배포 경계) — 이중계상 제거. api-surface §5 v1 amend + D8-A1 Built. |
| RQ-011 | role-QA / pipeline | sink_dlq.replay가 매트릭스/rbac.ts/web-permissions에 있으나 라우트 없음(공유 replay는 dlq.replay+workitem 전용, sink 행 not_replayable 거부). 운영자 sink replay capability 도달 불가 | P2 | confirmed | **[확정]** | `dlq.ts`(kind=sink 분기); `run-queue.ts`(enqueueSinkDeliver); `control-plane-contract.ts`(replaySinkDeadLetter); `api-sink-replay.int.ts`; `api-surface.md §4`; `release-decisions D8-A3`; PR#62 | **수정 완료(라우트 in-repo·CI green).** D8-A3 라우트 빌드: `POST /v1/dlq/{id}/replay?kind=sink` → sink_deliveries.status='dead_letter' 행 조회(RLS·미존재·비-dead_letter ⇒ 404) → in-handler `sink_dlq.replay` 인가(키 미소모 deny) → 멱등 tx에서 새 `sink_deliver` attempt 인큐(`replaySinkDeadLetter` 멱등 파티션) → 202 enqueued(전달 성공 가장 아님). **운영자 capability 도달 가능 = 원 finding 해소.** 검증 api-sink-replay.int 19. **실 재전달 sink egress는 RQ별 블로커 아니라 제품 전역 deploy-time row**(release-open-checklist; D6-2 — worker가 real port 미바인딩 시 SINK_DELIVERY_FAILED, 202는 전달 성공 가장 아님). resume3-prompt(동기 502) vs D8-A3(async 202) 상충 → 계약 D8-A3 채택. |
| RQ-012 | console-fidelity | StatusBadge가 한국어 라벨 대신 raw enum 텍스트 렌더(completed/failed_system/cancelled/dead_letter) — 비기술 한국어 register 위반(색상 tone은 정상) | P2 | confirmed | **[확정]** | `badges.tsx` STATUS_LABELS; `badges.test.tsx`; e2e 단언; PR#50 | **수정 완료(PR#50 머지·main green)**: StatusBadge에 STATUS_LABELS(Run/Workitem/HumanTask state+SITE_RISKS+approval/circuit, 목업 카피) 추가 → `{STATUS_LABELS[status] ?? status}`(미매핑 raw 폴백, 조용한 공백 금지). tone 미변경. badges.test 전수 완전성 가드 + console-browser/live e2e의 raw "running"→"실행 중" 단언 동반 교정. web 82 green. |
| RQ-013 | console-fidelity (a11y) | native `window.confirm`/`prompt` 사용 — 목업의 role=dialog aria-modal 포커스트랩 부재 | P2 | confirmed | **[확정]** | `ConfirmDialog.tsx`(신규); `ActionButton.tsx`; `HumanTasks.tsx`; `a11y.test.tsx`; PR#54 | **수정 완료(PR#54 머지·main green)**: 포커스 트랩 ConfirmDialog(role=dialog·aria-modal·Tab 순환·Esc·focus 복원) 신규 → ActionButton이 window.confirm 대체, optional inputLabel로 assignee 입력 다이얼로그(window.prompt 대체, 빈 값 확인 비활성). smoke 5 명령 테스트 다이얼로그 경유 갱신 + RQ-013 2 테스트 + a11y 다이얼로그-열림 axe 0 + console-live e2e 흐름 갱신. web 92 green, e2e green. |
| RQ-014 | tests-CI | 풀스택 D3 run pipeline 테스트(run-pipeline.int.ts, run-pipeline-site.int.ts)가 별칭만 있고 CI 체인 부재 — driver+Chrome+DB 통합 무게이트 | P2 | confirmed | **[확정]** | `contract-gates.yml` app-runtime 전용 단계; PR#55 | **수정 완료(PR#55 머지·main green)**: app-runtime에 "Run full-stack D3 run-pipeline integration"(test:pipeline-run+pipeline-site) 단계 추가 — test:int 직후, CHROME_PATH+비-bypass DB 재사용, test:int(순수-DB)와 분리·격리(플레이키↔회귀 구분). 로컬 결정적(6/6·7/7) + **CI Linux Chrome에서 green**. CDP=RQ-001 하든. |
| RQ-015 | tests-CI | `test:ci` 스크립트가 실제 게이트셋과 drift(lint·console e2e 누락) | P2 | confirmed | **[확정]** | `app/package.json`; `product-open-candidate-report.md`; PR#53 | **수정 완료(PR#53 머지·main green)**: test:ci(=test&&test:executor&&test:int)는 실 app-runtime 게이트의 strict subset(lint·console-e2e·console-live-e2e 누락)이라 "CI 게이트" 참칭 — 제거(소비자 0). report:167 test:ci 줄 제거(구성요소는 159·160·161·166에, full 게이트는 ci:local:temp-db@158에 이미 명시 → 거짓 증거 없이 손실 없음). 단일 CI-parity 러너=run-local-gates.mjs. grep test:ci=0(이력 제외), contract-lint 66·blocked-audit green. (confidence suspected→confirmed 승급: 실 delta 실측 확인.) |
| RQ-016 | contract-fidelity | `human_task.created`가 registry 등록 이벤트인데 in-repo producer 없음(미구현 ExecutorChallengeSuspensionPort에 위임) | P2 | confirmed | **BLOCK** | `event-payload-registry.ts:71,102`; `executor-completion-coordinator.ts:37-50,603-666`; `executor-invocation-recorder.int.ts:996-1007`(미배선 가드 테스트) | **BLOCK(미구현 의존).** Required decision/외부 소유자=D3 executor 스트림(codex): producer는 human_tasks INSERT와 분리 불가(이벤트는 row 생성 시점에 발행)이고, 그 INSERT는 challenge/exception suspend 경로의 createHumanTask side-effect를 통해서만 발생하는데 ExecutorChallengeSuspensionPort가 미구현(production 미배선, 음성테스트가 latent 가드). 더미 발행=문제 은폐 금지 위반. suspend 경로 구현 시 같은 tenant tx에서 emitOutboxEvent 배선. (confidence suspected→confirmed: producer 부재·port 미구현 grep 실측.) |
| RQ-017 | contract-fidelity | 인터프리터 `maxSteps` 기본 200 하드코딩(ops-defaults 미연동) | P2 | confirmed | **[확정]** | `ops-defaults.md §5`; `ir-interpreter.ts`; `release-decisions.md D8-A7`; README v2.15; PR#58 | **수정 완료(PR#58 머지·main green) — 계약 결정.** ops-defaults.md(=계약이 비운 수치의 SSoT)에 `interpreter.graph_max_steps`(200) 행 추가, **loop.max_iterations(loop body 전용)와 명시 구분**(이건 시작→terminal 총 노드순회 상한, 초과 시 `IR_LOOP_LIMIT`). 코드는 repo 규약(inline+`// ops-defaults §5` 인용; codex-sse/llm-gateway 등 동일)으로 연결. **값 불변(200) — 비발명**(가정 금지: 기존 가드를 추적가능화한 zero-behavior-change alignment). 결정=D8-A7. 검증: app typecheck·contract-lint 66·blocked-audit green + 값 일치(200↔200, 무회귀). |
| RQ-018 | contract-fidelity | `ir-translate` navigate가 `url_ref`를 그대로 `url`로 매핑 — `_ref`가 참조-해소 대상인지 리터럴인지 미확정 | P2 | confirmed | **[확정]** | `ir-translate.ts` `resolveUrlRef`; README v2.11; PR#42 | **해소(upstream, codex PR#42)**: url_ref는 run params의 키로 확정 — `resolveUrlRef(url_ref, params)`가 절대 URL 해소(params-key-only, URL_REF_* loud 에러), 리터럴 직매핑 버그 제거. site-match와 동일 함수(드리프트 없음). |
| RQ-019 | security | **GET /v1/artifacts/{id}가 mandatory `artifact.read` security-audit boundary append 없이 본문 반환(fail-open)** — RQ-010 [확정]의 §10 audit 의무 누락 | P1 | confirmed | **[확정]** | 재리뷰 wf_c10fd1d9 c3/r0; `reads.ts:468-496`; `security-contracts.md §10:147-148`; `SECURITY_AUDIT_REQUIRED_ACTIONS[0]='artifact.read'` | security-contracts §10은 artifact.read 결정을 **반환 전** append-only audit log에 fail-closed로 남기길 강제(append 실패 시 본문 반환 금지)인데, 라우트는 RBAC preHandler→RLS SELECT→artifactStore.get→200 으로 audit append 0건(ApiServerDeps에 writer 미주입). 라인149 scope-out 미해당(라우트 구현됨). **수정**: reads.ts에 `SecurityAuditDecisionWriter`(PgDurableSecurityAuditDecisionWriter, 이미 존재) 주입 + 본문 반환 전 recordDecision(artifact.read) fail-closed. 내 도메인(control-plane). **[확정](PR#67)**: ApiServerDeps.securityAudit 주입 + 본문 반환 전 recordDecision(artifact.read/allow) fail-closed(throw 시 본문 미반환); artifactStore 주입 시 writer 필수(미주입→registerReadRoutes throw). break-it 후속으로 allow-only audit scoping 명문화(RLS-404=존재 비노출·RBAC-deny=preHandler 별개 레이어). 검증 api-artifacts.int(disclosure 3행·404 미추가·미주입 throw). |
| RQ-020 | security | **Gateway prompt-injection detector가 영어 패턴만 매칭 — §3 한국어 instruction-override("너는 이제~")를 조용히 통과(fail-open)** | P1 | confirmed | **[확정]** | 재리뷰 wf_c10fd1d9 c3/r0; `gateway/redaction-boundary.ts:36`(DeterministicPromptInjectionDetector); `security-contracts.md §3 신호(b)` | `inspect()`가 `ignore previous/ignore system/system prompt` 영어 소문자 부분문자열로만 instruction_override 판정. §3가 canonical 예시로 명시한 한국어 "너는 이제~"(운영자/페이지 언어=한국어) 및 "ignore all previous instructions" 변형 미매칭→`clean`→adapter 전달. 와이어된 `DeterministicGatewayRedactionBoundary` 기본 detector. "조용한 unknown 금지" 위반. **수정**: §3 명시 패턴(한국어 지시-override 포함) 추가(계약 예시 추적, 비발명). gateway 공유 도메인. **[확정](PR#67)**: INSTRUCTION_OVERRIDE_PATTERNS 사전화(한국어 너는 이제/넌/이제부터·지시 무시 + 영어 비연속 ignore-all/you-are-now/from-now-on). break-it가 you-are-now 우회(자매 detector scaffold는 이미 차단) 발견→scaffold 정합 superset 보완. 검증 llm-gateway.unit(한국어·you-are-now·ignore-all 차단). **두 detector 단일 패턴 SSoT 통합=RQ-031**. |
| RQ-021 | security/correctness | **resolveUrlRef/originOf가 opaque-origin scheme(file:/javascript:/data:/blob:)을 절대 URL로 수용(fail-open)** — `new URL(x).origin`이 "null" 문자열 반환, `===null` 가드 무력 | P1 | confirmed | **[확정]** | 재리뷰 wf_c10fd1d9 c3/r0; `site-resolution.ts:44-50,71-73`; navigate 실행기도 scheme allowlist 부재 | `originOf('file:///etc/passwd')`는 JS null이 아니라 문자열 `"null"` 반환 → `if (originOf(value) === null)` 미발화 → file:/javascript:/data:/blob: 가 절대 URL로 통과. navigate 실행기 가드도 `new URL()`뿐(scheme allowlist 전무) → 경로 전체 fail-open(로컬파일/JS-URL nav 가능, §6 domain-allowlist 우회). 단위테스트 미커버. **수정**: originOf/resolveUrlRef에 http(s) scheme allowlist(fail-closed). ⚠site-resolution.ts=codex d3-multisite 신규 — 조율 필요. **[확정](PR#67, RQ-024 동반)**: originOf를 http(s) 제한(protocol≠http(s)→null) → resolveUrlRef·resolveSiteProfileId 가드 정상 발화. break-it 후속으로 navigate 실행기(utility-executor.ts)에도 동일 scheme 가드 추가(방어심층, 단일 producer 가정 비의존). 검증 site-resolution.unit·utility-executor.unit 신규. |
| RQ-022 | correctness | 가시 artifact의 object bytes 부재 시 500(CONTROL_PLANE_INTERNAL_ERROR) — fail-closed 404 아님 | P2 | confirmed | OPEN | 재리뷰 wf_c10fd1d9 c2/r1; `reads.ts:487`; `FsObjectStore.get` ENOENT throw; `errors.ts` 미분류→500 | `await artifactStore.get(object_ref)`가 try/catch 부재. 가시 행(redacted·미삭제)인데 retention/rollback(`pg-gateway-artifact-sink.ts:105 objectStore.delete`)이 bytes 제거한 창에서 ENOENT throw→미분류 500(원에러 로그 노출). D8-A1의 clean RLS 게이트 의도 위반. **수정**: object 부재→RESOURCE_NOT_FOUND(404, fail-closed). 내 도메인. |
| RQ-023 | security | redaction detector에 §3 신호(a) `hidden_instruction` emit 경로 전무 — 4신호 중 1 미구현인데 RQ-003 [확정] | P2 | confirmed | OPEN | 재리뷰 wf_c10fd1d9 c2/r1; `gateway/redaction-boundary.ts:27-78`; `security-middleware-contract.ts:304 "hidden_instruction"`(미사용) | `inspect()`가 instruction_override·credential_exfiltration·off_allowlist_url 3분기만, hidden_instruction 미반환. `redactForGateway`가 rawTextOrObject를 stableStringify로 평탄화해 가시성/DOM 좌표 소실→구조적 판별 불가. 항상 clean 통과("조용한 false 금지" 위반). **수정/Required decision**: 호출자 visibility 메타 스레딩 + hidden 신호 보수적 차단(구조 변경 필요). gateway 도메인. |
| RQ-024 | correctness | resolveSiteProfileId가 opaque "null" origin 우연일치로 무관 site_profile 매칭(+ null-guard 무력) | P2 | confirmed | **[확정]** | 재리뷰 wf_c10fd1d9 c3/r0; `site-resolution.ts:138-150`; README v2.10 L480-481(origin identity) | RQ-021과 동근(originOf "null" 문자열). line139 null-guard가 opaque scheme에 미발화, line150 후보필터 `originOf(url_pattern)===origin`이 "null"==="null"로 매칭 → opaque entry URL이 무관 profile(잘못된 selector/identity/network policy)에 해소. **수정**: RQ-021과 함께 scheme allowlist + origin identity 강화. codex 도메인 조율. **[확정](PR#67, RQ-021 동반)**: originOf http(s) 제한으로 opaque "null" 문자열 오매칭 차단 — line139 null-guard 정상 발화(질의 전 loud throw)·line150 후보필터 비매칭. 검증 site-resolution.unit(opaque entry→URL_REF_SYMBOLIC_UNRESOLVED 질의 전). |
| RQ-025 | contract-fidelity | openapi.yaml getArtifact가 pre-D8-A1 409/403 선언, 구현이 반환하는 404 미선언(codegen drift) | P2 | confirmed | **[확정]** | `codegen/openapi.yaml`(getArtifact responses); `api-surface.md §5`; PR | **수정 완료(codegen·spectral/consistency green).** getArtifact responses에서 **`409`(ARTIFACT_NOT_REDACTED) 제거**(D8-A1 v1 미노출) + **`404`(RESOURCE_NOT_FOUND) 추가**(v1 redaction-gate=RLS) + 403(SECRET_ACCESS_DENIED) 유지 + description을 D8-A1 v1 동작으로 갱신. 비고: openapi.yaml은 **생성기 없는 수기 D1 산출물**(codegen/package.json엔 spectral lint만, 생성 스크립트 부재) — "재생성"=amend된 api-surface §5 + 구현에 맞춰 수기 정합이 곧 재생성. 검증 spectral 0 errors·codegen test(fixtures 87·validators 42·consistency) green. |
| RQ-026 | console-fidelity | badges.tsx HumanTask `open`(열림)이 실패색(RED)으로 렌더 — circuit `open`과 tone 키 충돌(RED가 BLUE보다 우선) | P2 | confirmed | **[확정]** | `badges.tsx`(tone+kind); `Security.tsx`(circuit kind); `badges.test.tsx`; PR | **수정 완료(console·CI green).** `tone()` RED Set에서 `"open"` 제거 → **HumanTask `open`(열림)=BLUE(중립)** 정상화. 같은 enum 문자열이 도메인별 다른 tone이라 `StatusBadge`에 optional `kind?: "circuit"` 추가 → **circuit `open`=RED(경보)** 분리(Security 서킷 배지가 kind 전달). 라벨("열림") 불변(e2e 무영향). 검증 badges.test 2 case(humantask open→blue·not red, circuit open→red) + web typecheck·94 test green. |
| RQ-027 | console-fidelity | Gateway.tsx 다중 정책 테넌트 dead-end — "모델 선택" 안내만, 선택 UI 없음 | P2 | confirmed | OPEN | 재리뷰 wf_c10fd1d9 c2/r0; `Gateway.tsx:12,22-24,44-45`; `reads.ts:402-404`(다건+model 미지정→422 model_required) | `getGatewayPolicy()`를 model 없이 호출→다중 정책 테넌트에서 422(model_required)→"모델을 선택하면 상세 표시" 안내만 띄우나 모델 셀렉터/`getGatewayPolicy(model)` 호출 경로 전무. admin도 편집 폼 도달 불가(영구 차단). **수정**: 모델 셀렉터 UI + model 전달. console 도메인. |
| RQ-028 | tests-CI | in-handler `sink_dlq.replay` 인가(RQ-011)가 어떤 테스트로도 미실행 — 매트릭스 co-grant로 deny 분기 미커버 | P3 | confirmed | OPEN | 재리뷰 wf_c10fd1d9 c2/r1; `api-sink-replay.int.ts:213-217`; `dlq.ts:50-61`; `rbac.ts`(dlq.replay↔sink_dlq.replay 동일 역할집합) | viewer 거부는 route preHandler(dlq.replay)에서 먼저 차단→in-handler sink_dlq.replay deny 분기 미도달. dlq.replay 보유·sink_dlq.replay 미보유 토큰 부재로 게이트 미검증(블록 제거해도 19 check 통과). **수정**: 매트릭스-분기 principal로 deny 경로 단위테스트 추가. 내 도메인(test-only). |
| RQ-029 | security | structured-output repair(repairOnce)가 redaction/injection 우회 — priorText를 redactRequest 미경유 재주입 | P3 | confirmed | OPEN | 재리뷰 wf_c10fd1d9 c2/r1; `llm-gateway.ts:304-327` | 정상 경로는 adapter 전 redactRequest(모든 user 메시지)인데, §5 repair가 priorText(모델 출력=비신뢰)를 새 user 메시지로 append 후 `consumeOnce` 직접 호출(redactRequest 미경유). 모델이 echo한 민감/주입 텍스트가 redaction/injection 검사 없이 adapter 전달. **수정**: repairReq도 redactRequest/injection 경유. gateway 도메인. |
| RQ-030 | contract-fidelity | openapi getArtifact 200 Artifact 스키마가 구현 필드(type/sha256/retention_until) 누락 + 미반환 signed_url 선언 | P3 | confirmed | **[확정]** | `codegen/openapi.yaml`(Artifact schema); `reads.ts`(반환 필드); PR | **수정 완료(RQ-025와 동반, codegen green).** Artifact 스키마를 구현 반환에 정합: **`type`·`sha256`·`retention_until`(date-time) 추가**, **`signed_url` 제거**(impl은 inline content 반환), `content`에 `type: string` 부여, redaction_status 설명의 폐기된 409 참조를 404로 교정. 최종 스키마=`{artifact_id, type, sha256, redaction_status, retention_until, content}`(reads.ts 반환과 일치). 검증 codegen test green. |
| RQ-031 | security/contract-fidelity | §3(b) instruction-override를 구현하는 두 detector(gateway/redaction-boundary.ts vs security/compliance-scaffold.ts)가 패턴 사전 분기 — 동일 텍스트가 경로별 상이 판정 | P2 | confirmed | OPEN | break-it wf_c5cdd6d9; `gateway/redaction-boundary.ts` INSTRUCTION_OVERRIDE_PATTERNS vs `security/compliance-scaffold.ts:383` INSTRUCTION_OVERRIDE_RE | RQ-020 break-it 파생. RQ-020 수정으로 gateway가 scaffold 토큰을 superset 포함해 **wired 경로 비대칭은 해소**됐으나, 두 detector가 같은 §3(b) SSoT를 별도 사전으로 구현해 재발 위험 잔존. 각자 fixtures(gateway/security)가 통과해 자동 게이트로 불일치 미포착. **수정**: §3(b)/§3(c) 패턴을 단일 export 상수로 통합·양 detector 참조(또는 scaffold의 deterministicPromptInjectionDetector를 gateway가 위임). 구조 정리(낮은 위험, 별 PR). |

## 처리 로그 (append)
- 2026-06-16: 레지스터 생성. 18 이슈 등록(워크플로우 12 + 스팟 6). 전부 OPEN/BLOCK — 완료 아님("사람 검토 대기"). 다음: RQ-001부터 backend 우선 수정.
- 2026-06-16: **RQ-001 [확정]** — cdp-session 기동 retry/backoff + DI 단위테스트. CI 안정 키스톤. 다음: RQ-008(ir-translate drift)·RQ-009(§3vs§5 계약 개정)·RQ-002(on[] scope).
- 2026-06-16: **RQ-008 [확정]** — ir-translate compiled_ast 드리프트 → IR_SCHEMA_INVALID + 단위테스트 5/5. 다음: RQ-009(§3vs§5 계약 개정)·RQ-002(on[] scope full).
- 2026-06-16(resume): **RQ-009 [확정]** — ir-expression §3 내부모순 교정(null 수치→IREL_RUNTIME_MISSING, README v2.12). **RQ-018 [확정]** — codex PR#42가 url_ref→params 해소 구현(upstream). 비고: **RQ-002**는 codex v2.11 "연기" 행에 disclosed deferral로 문서화됨(인터프리터 on[] scope에 flags만 주입 — params/node/cursor 분기는 별도 증분). [확정] 4/18, BLOCK 2. 다음: RQ-002(on[] full scope, P1) 또는 RQ-007/014/015(CI 게이트).
- 2026-06-16(resume): **RQ-001 후속 확장** — #45 main run에서 CDP 기동 플레이키의 *타임아웃 변종*(`ConnectionTimeoutError`/"Timed out waiting for CDP websocket")이 첫 fix(ECONNREFUSED만)에 안 잡힘 발견 → `isRetryableLaunchError` 패턴에 connection-timeout/websocket-timeout 추가(code/name/message 매칭) + 단위 case 추가(5/5). 은폐 없이 기록(첫 fix 불완전 → 확장).
- 2026-06-16(resume): **RQ-003 [확정]** — Gateway §4 step2 redaction/injection 경계 배선(LlmGatewayDeps.redactionBoundary required, call() adapter 전 redactForGateway, injection→PROMPT_INJECTION_DETECTED 차단·secret→[REDACTED]). 단위 3 case green. 잔여 후속(비-보안): executor cast 제거·networkPolicy 도메인 체크. [확정] 5/18. 다음: RQ-004(R5 kind) 또는 RQ-007/014/015.
- 2026-06-16(resume): **RQ-004 [확정]** — R5 humanTaskKind 하드코딩(RBAC 오라우팅) 교정: RunEvent에 humanTaskKind 추가(계약)+R5 전파(codegen)+fixture 87/0(kind 단언)+state-machine.md R5 문서화. [확정] 6/18. 다음: RQ-007(site-page-state CI gate) 또는 RQ-014/015(orphan/test:ci) — backend P2 저충돌; 프론트(RQ-005/006/012/013)는 codex 콘솔 스트림.
- 2026-06-16(resume2): **재검증 워크플로우**(wf_ea574dda, 12 이슈 origin/main 008f9af4 대조) — 전 이슈 stillPresent 재확인 + 최소수정·충돌위험 산출. 이후 7개 [확정] 머지:
  - **RQ-007 [확정]**(PR#49) — site-page-state.int를 test:executor 편입. **RQ-012 [확정]**(PR#50) — StatusBadge 한국어 라벨(+e2e raw-status 단언 동반 교정, web vitest 미포착분을 CI가 노출→수정). **RQ-006 [확정]**(PR#51) — admin gateway 편집 UI(GET ETag=version 백엔드 동반). **RQ-005 [확정]**(PR#52) — openGate 정적 뷰. **RQ-015 [확정]**(PR#53) — test:ci 제거. **RQ-013 [확정]**(PR#54) — 포커스 트랩 ConfirmDialog. **RQ-014 [확정]**(PR#55) — 풀스택 run-pipeline CI 편입(Linux Chrome green).
  - 각 PR CI green→머지→main run conclusion=success 재확인. 병렬 codex 공존: worktree(rpa-fl) 전용 브랜치·path-scoped·머지 전 rebase.
- 2026-06-16(resume2): **분류 정정(은폐 금지)** — RQ-002/016/017을 OPEN→**BLOCK**으로 강등(전부 confirmed). 사유: 셋 다 *미정 계약* 또는 *codex 소유 미구현 증분* 의존이라 in-repo 결정적 수정 불가(값/계약 발명 금지). RQ-010/011은 BLOCK 유지(재확인). **결과: [확정] 13 · OPEN 0 · BLOCK 5.** OPEN 0 달성했으나 BLOCK 5 잔존 → **NEEDS HUMAN REVIEW**(완료 아님). BLOCK 5는 전부 Required decision/외부 소유자로 1:1 추적(상태 요약 참조).
- 2026-06-16(resume2): **RQ-002 부분 배선(PR#57, opus)** — BLOCK 강등된 RQ-002 중 **계약 결정 불요·충실 도출 가능 subset**을 배선: selectOnBranch 스코프 `{flags}`→`{flags, params, node:nodeScope}`로 **params.* + node.<id>.status**(status=StepResult.status 단일출처). 워크플로 설계+적대검증(verify#4 node-missing↔field-absent 구분·res 블록스코프·graph-ancestor≠executed 사전포착). interpreter-scope.unit 5(params 분기·node.status·부재노드·field-absent on populated record). **잔존은 BLOCK 유지**=node.{extracted_ref,row_count,tier} 투영+cursor/loop(미정 versioned contract, 가정 금지) → 미투영 참조 IREL_RUNTIME_MISSING(loud, compile-then-throw 의도). RQ-002 resolution=부분/BLOCK(카운트 BLOCK 5 불변).
- 2026-06-16(resume2): **계약 결정 진행** — 사용자 지시("계약 결정 필요한거 진행"). 3개 BLOCK 중 진행 가능성 판정: **RQ-002**=codex 활성 예약 증분(무접촉), **RQ-010**=계약 이미 결정(D8-A1=404, §5 amend는 "when built" 연기)+외부 egress만 잔존(결정할 계약 없음), **RQ-017**=내 권한 내·closeable → **RQ-017만 진행**. **RQ-017 [확정]**(PR#58 머지·main green): ops-defaults.md §5에 `interpreter.graph_max_steps`(200, loop.max_iterations와 구분) 계약 행 + 코드 inline-인용 연결 + D8-A7 + README v2.15. **값 불변(200)=비발명** zero-behavior-change alignment(가정 금지). **결과: [확정] 14 · OPEN 0 · BLOCK 4** → 여전히 NEEDS HUMAN REVIEW. 잔존 BLOCK 4(RQ-002·010·011·016)는 전부 외부 egress/codex 미구현·예약 — **내 권한 내 계약 결정은 모두 소진**.
- 2026-06-16(resume2): **RQ-002 잔존 투영 확정·구현(opus, README v2.16) — codex 예약 override(운영자 권위)** — codex가 위에서 RQ-002를 "codex 활성 예약 증분(무접촉)"으로 둔 직후, **운영자가 투영 규약을 직접 결정·진행 지시**("옵션2로 진행해"): extract 출력 `{rows:[...]}` 봉투(LLM 구조화 출력=루트 object라 루트 배열 불가). `row_count=output.rows.length`·`extracted_ref=artifacts[0]`·verify min_rows 동일 `rows` 카운트, ir-expression §2 명시. 워크플로 설계+적대검증(option1=루트배열 불가 사전포착→option2). 구현: dom-executor rowCount + 인터프리터 projectNodeOutput(extract만). 검증 interpreter-scope.unit 8·dom-executor unit·**interpreter-llm(실 Chrome extract {rows:[1,2,3]}→row_count=3→분기→done)**. **→ RQ-002 BLOCK 해소(부분)**; 잔존 tier/cursor/loop=feature-gated. **결과: [확정] 14 · OPEN 0 · BLOCK 3(RQ-010/011/016) · 부분 1(RQ-002).** ⚠codex 진행 중 RQ-002 잔존 설계 있으면 이 결정과 정합 필요(운영자 조율).
- 2026-06-16(resume3): **재확인** — origin/main `5c3580d2` green, 스냅샷 이후 codex의 loop/fallback/suspend 증분·외부 egress 인프라 도입 **없음**(4건 잔존 신호 무). **RQ-011 라우트 partial-build([확정])** — D8-A3 in-repo 빌드: `POST /v1/dlq/{id}/replay?kind=sink` → sink_deliveries.status='dead_letter' 행 조회(RLS·미존재·비-dead_letter ⇒ 404) → in-handler `sink_dlq.replay` 인가(키 미소모) → 멱등 tx에서 새 `sink_deliver` attempt 인큐(`replaySinkDeadLetter` 파티션, `enqueueSinkDeliver`) → 202 enqueued. 변경: control-plane-contract(OperationId+CommandEndpoint)·run-queue·dlq.ts·api-surface §4·release-decisions D8-A3 Built 노트 + RunEnqueuer test double 13곳에 no-op 메서드. 검증 api-sink-replay.int 19(operator 202+인큐 식별자·viewer 403 키미소모·cross-tenant 404·absent/delivered 404·invalid kind 422·키누락 422·멱등 1회·workitem 회귀) + app/codegen typecheck·contract-lint 67·unit 92·console-e2e·full test:int(temp PG) green. **⚠설계 충돌 해소(은폐 금지)**: resume3-prompt 스케치는 *동기 deliver→502*였으나 계약 **D8-A3는 async enqueue→202**(enqueuer 메서드·"worker가 port 사용" 명시). release-decisions=SSoT이고 control/data-plane 경계와 정합이라 **D8-A3 채택**(202=accepted, 전달 성공 가장 아님). **운영자 capability 도달=원 finding 해소; 실 재전달은 egress(D6-2/B3) BLOCK 잔존.** **결과: [확정] 14 · OPEN 0 · BLOCK 2(RQ-010·016) · 부분 2(RQ-002·011)** → 여전히 NEEDS HUMAN REVIEW. 잔존(RQ-010 egress·RQ-011 egress·RQ-016 codex suspend 경로)은 전부 외부 egress/codex 미구현 — in-repo로 닫을 수 없음.
- 2026-06-16(resume3): **RQ-010·016 진행 지시(사용자)** — 차원별 feasibility 워크플로(wf_ed4eb08b) 후 판정. **RQ-016=partial 불가, BLOCK 유지**: producer는 human_tasks INSERT와 분리 불가, 그 INSERT는 suspend/challenge 경로(R4→R11 + resume-token 발행 + @challenge/@human_task 핸들러 I/O + 계약-적재 kind/assignee_role/expires_at/payload_ref)에서만 발생 → 미구현 ExecutorChallengeSuspensionPort 종속(codex executor). 더미 INSERT+발행은 run을 'suspending' 반-상태로 좌초(조용한-false/편법 금지 위반); 음성 테스트가 이미 fail-honest 가드. recommendation=too-large-defer. **RQ-010 부분(라우트 [확정])** — RQ-011과 동일 injected-port 패턴: GET /v1/artifacts/{id} 빌드(reads.ts) — artifact.read RBAC + `artifacts_visible_isolation` RLS를 redaction 게이트로(redacted/not_required·미삭제·비격리만 가시) → pending/failed/quarantined/deleted/cross-tenant ⇒ 404(D8-A1, BYPASSRLS 미사용, 409 미노출). 200 본문 = injected `ArtifactObjectReader`(`ObjectStore.get` 추가; api는 narrow 인터페이스에만 의존=단방향). artifactStore 미주입 시 라우트 미등록. **운영자 artifact-read capability 도달=원 finding 해소**; 잔존 = 실 **분산 object-store(S3) 바인딩 deploy-time(B3)**(sink egress·outbox real-bus 동일 posture). api-surface §5 v1 404 amend(D8-A1 "when built" 이행) + D8-A1 Built 노트 + README v2.17. 검증 api-artifacts.int 12 + app typecheck·contract-lint·full test:int(temp PG) green. **결과: [확정] 14 · OPEN 0 · BLOCK 1(RQ-016) · 부분 3(RQ-002·010·011)** → 여전히 NEEDS HUMAN REVIEW. 남은 in-repo 작업: 없음(RQ-016=codex suspend, RQ-002 잔존=loop/fallback, RQ-010·011=deploy-time egress 바인딩).
- 2026-06-16(resume3-2): **분류 정정 — RQ-010·011 부분→[확정](이중계상 제거)** — 사용자 지적("외부 인프라는 없다"). 검토 결과 정당: RQ-010(object-store)·RQ-011(sink egress)의 "실 바인딩"은 **RQ별 블로커가 아니라 제품 전역 deploy-time port 경계**다. release-open-checklist 프레임워크가 이미 (a) **Repo-controlled rows**(L60 — repo 코드+테스트+CI 게이트로 닫힘)와 (b) **deploy-time/Hybrid rows**(L43–52·L59–61 — SecretRef-backed 실 object-store/sink 자격은 배포 시 owner가 바인딩)를 분리 추적한다. 두 라우트의 repo 산출물(빌드+테스트+CI-green)은 (a)로 완결이고, 실 바인딩은 object-store·sink·outbox-bus·SecretStore에 **균일 적용되는 (b)**에 이미 잡혀 있다 — per-issue BLOCK으로 다시 센 것은 **이중계상**이었다. 원 finding("라우트 미구현→capability 도달 불가")은 in-repo로 완전 해소(api-artifacts.int 12·api-sink-replay.int 19·CI green). 코드 변경 없음(레지스터 분류만 정정). **결과: [확정] 16 · OPEN 0 · BLOCK 1(RQ-016) · 부분 1(RQ-002).** 잔존 2건은 **외부 인프라 아님 = codex 미구현 feature**(RQ-016 suspend/challenge 증분, RQ-002 잔존 loop/fallback) — 빌드가 codex 활성 D3 도메인이라 내가 닫으면 충돌·중복.
- 2026-06-16(resume3.5): **적대적 재리뷰 + 신규 12건 등록(RQ-019–030, ultracode)** — 기존 18건이 전부 [확정]/외부BLOCK이라 in-repo 클로즈 작업 소진 → 원 리뷰 베이스라인 `212e18cc` 이후 머지된 ~1240줄 델타(특히 갓 [확정]된 RQ-010/011 라우트·RQ-003 redaction·신규 site-resolution.ts +162·composite-executor.ts)를 격리 워크트리(rpa-review @96f689eb)에서 8차원 적대 리뷰(wf_c10fd1d9, 56 agents). 결함당 3 회의적 검증자(reproduce/contract/impact 렌즈, 과반 refute 폐기). **16 findings → 12 생존·4 false-positive 폐기**(폐기: sink replay 404 키소모=계약 허용·reachability traversal=계약 위반 아님·sleep 타이머 누수=무해·ReplayResult 스키마=validator 미구성). P1 3건은 직접 스팟리뷰로 코드+계약 재확인(security-contracts §10:147-148 audit 의무·§3 한국어 신호·WHATWG opaque-origin "null" 문자열). 전부 confirmed·OPEN(미수정). 우선순위 P1 3건(RQ-019·020·021). **여전히 NEEDS HUMAN REVIEW.** ⚠레지스터 동시편집 회피: 병렬 세션이 fl/reclassify-deploy-rows에서 open-issues.md 미커밋 편집 중임을 발견(rpa-fl `git status` 재확인)→그 컨텍스트 복원 후 전용 워크트리(rpa-reg)에서 append-only로 작업.
- 2026-06-16(resume3.5): **RQ-026 [확정]** — 사용자 지시("P1은 다른 세션 진행 중, 비충돌 거 찾아 진행"). P1 트리오 파일(reads.ts/redaction-boundary.ts/site-resolution.ts) 회피 → **RQ-026**(badges.tsx tone 충돌, 내 RQ-012 영역, console, P1과 완전 분리) 선택. 수정: `tone()` RED Set에서 `"open"` 제거(HumanTask 열림=BLUE 정상) + `StatusBadge`에 optional `kind?: "circuit"` 추가해 circuit `open`=RED(경보) 분리(Security 서킷 배지 kind 전달). 라벨 불변→e2e 무영향. badges.test 2 case + web typecheck·94 green. **[확정] 17·OPEN 11.** (P1 RQ-019·020·021은 다른 세션 소유; RQ-022 reads.ts·023 redaction·024 site-resolution도 P1 파일 충돌로 회피.)
- 2026-06-16(resume3.5): **P1 3건 + RQ-024 [확정](PR#67, ultracode)** — 재리뷰 P1 트리오 수정: **RQ-019**(reads.ts: artifact.read를 §10 audit boundary로 fail-closed 게이트, writer 미주입→라우트 등록 throw) · **RQ-020**(redaction-boundary.ts: §3 한국어 "너는 이제~"+영어 비연속/you-are-now instruction-override 사전화) · **RQ-021/024**(site-resolution.ts: originOf http(s) 제한 — opaque scheme "null" 문자열 fail-open 차단). **⚠적대적 break-it 검증(wf_c5cdd6d9, 수정당 3 회의적 검증자)이 실측 갭 발견→같은 PR 보완**: RQ-020 you-are-now 우회(자매 detector scaffold는 이미 차단)→scaffold 정합 superset 확장; RQ-021 navigate 실행기(utility-executor.ts) scheme 가드 부재→방어심층 추가(+utility-executor.unit 신규); RQ-019 allow-only audit scoping 명문화. **신규 RQ-031**(두 §3(b) detector 패턴 사전 분기—단일 SSoT 통합 권고) 등록. 검증: app/codegen typecheck·test:unit(한국어·you-are-now·scheme green)·codegen fixtures 87/0·contract-lint·**full test:int(temp PG) green**·CI green→머지(PR#67). **결과: 총 31 — [확정] 21 · OPEN 8 · BLOCK 1 · 부분 1** → 여전히 NEEDS HUMAN REVIEW. 병렬 세션과 P1 파일/레지스터 충돌 회피(전용 워크트리 rpa-fix·rebase). 잔여 OPEN 8=P2/P3(console RQ-027·codegen RQ-025/030·gateway RQ-023/029·test RQ-028·RQ-031) + codex 2(RQ-016/002).
- 2026-06-16(resume3.5): **RQ-025·030 [확정]** — 사용자 지시("openapi getArtifact 정합 이어서"). 둘 다 내 RQ-010 라우트의 openapi drift(`codegen/openapi.yaml`, reads.ts와 파일 분리→RQ-019 세션과 비충돌). **RQ-025**: getArtifact responses 409 제거(D8-A1 v1 미노출)+404 추가+description v1 RLS-게이트로. **RQ-030**: Artifact 스키마 type/sha256/retention_until 추가+signed_url 제거+content `type:string`→`{artifact_id,type,sha256,redaction_status,retention_until,content}`(reads.ts 반환 일치). **비고**: openapi.yaml은 생성기 없는 수기 D1 산출물(codegen에 spectral lint만)이라 계약(api-surface §5 amend)+구현 정합이 곧 "재생성"(row의 "hand-edit 금지"는 생성기 가정 — 실측 부재). 검증 yaml-parse·spectral 0 errors·codegen test(fixtures 87·validators 42·static 33·consistency) green·contract-lint 67. **[확정] 23·OPEN 6.** (병렬 PR#68과 레지스터 rebase 충돌→최신 베이스에 RQ-025/030 [확정] 재적용.)
