# UI/UX 재설계 전량 구현 — 진행 레지스터 (append-only)

**✅ 미션 완료 (2026-07-10)** — 전 슬라이스 머지: T1~T8(8), R1~R4·R6(5, R5=STALE), E0~E7(8, 일부 STALE 축소 — 근거는 각 행). PR #422~#441(20개), main green 연속. 감사 P0 4건·P1 5건·P2군 전부 해소 슬라이스와 매핑됨.

미션: E0~E7 · R1~R6 · T1~T8 전량 구현. 설계 SSoT·규칙은 아래 3문서(재논의 금지).

- `docs/rpa-easy-authoring-detailed-design-2026-07-09.md` (E계열)
- `docs/rpa-console-simplification-review-2026-07-09.md` (R계열)
- `docs/rpa-console-uiux-audit-remediation-design-2026-07-10.md` (T계열 + E/R 수용 기준 보강 §7)

규칙 요약: 1 슬라이스 = 1 브랜치 = 1 PR(origin/main에서 분기, 스택 금지) → typecheck/test/build green →
PR → CI green → 머지 → main green 확인 후 다음. 문서 file:line은 착수 직전 재추적. 계약 루트 무변경.

## 착수 전 검증 포인트 결과 (2026-07-10 확인)

| # | 항목 | 결과 |
|---|---|---|
| T§8-① | DEAD_LETTER 라벨 폴백 | **확정** — `badges.tsx` STATUS_LABELS는 소문자 `dead_letter`만 보유, `statusLabel()`이 대문자 키에서 raw 폴백(`badges.tsx:26-55`). tone은 정상(RED set에 대문자 포함). 해결: 조회 시 소문자 정규화 폴백(T4) |
| T§8-② | run 소요 시간 필드 | **부재** — `RunItem`(`types-runs.ts:7-19`)은 run_id·as_of·updated_at?뿐, duration은 StepSummary에만. → T6 소요 열 **보류**, 실행 번호(run_id 축약) 열 + updated_at 상대 표기로 대체(날조 금지) |
| T§8-③ | Gateway 비용 한도 단위 | **USD 확정** — `ops-defaults.md:103` `llm.budget.max_cost_per_run` $0.85(run 누계 상한) → T3에서 "$" 병기 |
| T§8-④ | readiness 벨 문구 | **가능** — `summary.blocker_count` 존재(`TopbarActions.tsx:151`) → "운영 전환 준비 차단 N건" |
| T§8-⑤ | automationOps section 딥링크 | **소비 확정** — `Orchestration.tsx:96` `useHashParam("section")` |
| E§12-① | url_ref 해석 | **심볼릭 키 확정** — seed IR `"entry_url"`/`"orders_url"`(`app/dev/seed-scenarios.ts:124,154`). StepCards는 사이트 데이터로 해석 시도, 불가 시 키 원문 |
| E§12-② | planner studio_mode 방출 | **확정** — `app/src/api/scenario-generation-planner.ts:204` `studio_mode:"easy"` |
| E§12-③ | session.capture 역할 | rbac-policy.ts 내 5곳 존재 — 역할 포함 범위는 E3 착수 시 라인 문맥 확인 |
| E§12-④ | runScenarioGeneration run_mode | **미확정** — 설계 기본 채택: 보정 후에도 `createRun(run_mode:"test")` 경로 통일, E4 착수 시 재확인 |
| E§12-⑤ | 템플릿 목록 API | **확정** — `api.listConnectors`/`api.listTemplates`(`ConnectorCatalog.tsx:77,112`) |

## 슬라이스 진행

| 슬라이스 | 상태 | PR | 검증 | 특이사항 |
|---|---|---|---|---|
| T1 상단바 재설계 | ✅머지 | #422 | web 988·e2e 10/10·CI 11/11 | 계정 팝오버 통합·컨텍스트 배지 중립화·알림 벨 신설·create 메뉴 생성 전용·e2e 1280/1440 가드 |
| T2 알림 그룹핑 일원화 | ✅머지 | #423 | web 990·CI 11/11 | OpsSignalPanel groupOpsAlerts 소비(limit 50)·모두 보기 링크·값-상태 분리(대시보드+automationOps 스케줄러) |
| T3 표기 규칙 | ✅머지 | #424 | web 990·CI 11/11 | Workitems raw ISO→formatDateTime·N건 통일·Gateway 폼 단위(토큰·USD/실행)·저장/편집 구분 캡션. PolicyReadout 단위는 기구현 확인 |
| T4 용어 교정+copy-gate | ✅머지 | #425 | web 991·lint:copy·CI 11/11+게이트 | statusLabel 소문자 정규화·문구 교정 12곳·tools/copy-gate.mjs+allowlist(OpenGate·ENV키 2종)+CI 스텝 |
| T5 사람 확인 결정 우선 | ✅머지 | #426 | web 996·CI 12/12 | [승인][반려] 단축(approval·비구조화·체인 단건 재사용)·0-타일 조건 렌더·필터/일괄 2줄 분리·principalLabel 식별자+이름 미등록 |
| T6 실행 기록 식별성 | ✅머지 | #427 | web 997·CI 12/12 | 실행 번호 #8자 병기(미노출 정책 감사 근거로 개정, 전체 번호는 툴팁 유지)·updated_at 툴팁·우선순위 셀 고정폭. 소요 열 보류(§8-②) |
| T8 표면 하드닝 | ✅머지 | #431 | web 994·e2e 10/10·CI 12/12 | .btn nowrap·section-tabs 한줄 스크롤(automationOps+Security 공용)·사이드바 스크롤 그림자·도입증빙 라벨 분리+문장부호·coe 프리필→placeholder+타일 출처 명시·Workitems 처리현황 열 |
| R2 irValidation 은퇴 | ✅머지 | #428 | web 995·CI 12/12 | 뷰 삭제·5곳 등록 해제·#irValidation→#scenarioStudio·ReadinessCard 검사 화면 링크 제거·은퇴 테스트 3건 삭제 |
| R3 idempotency 은퇴 | ✅머지 | #429 | web 994·CI 12/12 | 뷰 삭제·5곳 등록 해제(리다이렉트 불요=기본 뷰 폴백)·hidden-view 테스트 표본을 coePipeline로 교체 |
| R5 Dashboard adoption 탭 | ✅STALE(선반영) | — | 실측 확인 | 도입 패널 3종은 이미 별도 뷰(adoptionEvidence)로 분리·Dashboard 미렌더(Phase 7에서 이동됨). 목표 동등 달성 — 작업 불요 |
| R6 automationOps today 삭제 | ✅머지 | #430 | web 994·CI 12/12 | today 탭·렌더 삭제, 기본=schedule, 정적 저장가능/준비중 컬럼 삭제, 운영 헬스 상세는 큐 섹션으로 이동 |
| E0 준비판정·모델판별 공용화 | ✅머지 | #432 | web 994·CI 12/12 | siteReadiness는 STALE(components/readiness.ts에 기추출) — model_required 판별만 공용 추출(run-scenario/model-required.ts) |
| E1 랜딩·내비 전환+확인 스트립 | ✅머지 | #433 | web 994·e2e 10/10·CI 12/12 | DEFAULT_VIEW=create·navMode §2.3 슬림(viewer std 5·operator std 8)·확인 스트립 이관(review-queue 공용 훅)·팔레트 자동화 딥링크 create 소유·생성기 mode 딥링크 동기화 결함 수정. 홈 전면 재구성은 E2와 병행 |
| R4 myWork 은퇴 | ✅머지 | #434 | web 988·e2e 10/10·CI 12/12 | 뷰 삭제·5곳 해제·#myWork→#create·확인 큐는 E1 스트립이 흡수 |
| E2 번역기+StepCards | ✅머지 | #435 | web 1010·CI 12/12 | step-sentences(§5 전수 22케이스)+StepCards. 소비면 2곳 배선: 초안 미리보기(GenerationResult)·계획 미리보기(workbench Plan 대체=T7 절반). url_ref는 기존 urlRefLabel 공유. 위저드 셸·useEasyGeneration은 E3/E4로 |
| E3 준비 확인 인라인 | ✅머지 | #438 | web 1015·CI 12/12 | 대부분 STALE(사이트 인라인 등록·blocked 보정 가이드는 Phase 7 선반영) — 잔여=세션 등록 인라인화(corridor CTA→CaptureGuide 다이얼로그, 화면 이동 제거) |
| E4 인라인 테스트 진행 | ✅머지 | #436 | web 1014·CI 12/12 | TestProgress(getRun/steps 폴링+SSE, 배너 §4.7, 9상태 오버레이)·RunScenarioButton onStarted·워크벤치 인라인(run 해시 보존, 화면 튕김 제거) |
| R1 playground 은퇴 | ✅머지 | #437 | web 1014·CI 12/12 | 뷰키 5곳 해제(리다이렉트·워크벤치·테스트는 기존 유지) |
| E5′ 설계 탭 실초안 | ✅머지 | #441 | web 1015·CI 12/12 | 집중 스튜디오 설계 탭에 StepCards(scenario-detail 공유). 말로 고치기=P1 확정(원본 프롬프트 접근 계약 부재 — 설계 §13 revision API 후보 유지) |
| E6 템플릿 갤러리 | ✅머지 | #440 | web 1015·CI 12/12(픽스처 타입 1회 수정) | 홈 갤러리(listTemplates+카탈로그 프리필 계약 재사용). creator 파라미터=STALE(Phase 7이 소비 로직 신설). easy-labels 사전=T4 게이트로 최소선 충족, 위저드 셸 부재로 보류(설계 개정 기록) |
| E7+T7 스튜디오 액션 재편 | ✅머지 | #439 | web 1015·CI 12/12 | 행 [열기][실행]+더 보기(테스트 계획·편집·이력·배포·운영 기준·보관), 용어 단일화(테스트), 실행 기준 단정 배지 열 삭제 |
| F6 step-sentences 폴백 정밀화 | ✅머지 | #443 | web 1020(step-sentences 22→27)·lint:copy·CI 11/11 | 객체형 예약 핸들러({handler}) 문장 재사용+"[object Object]" 제거·fallback_chain 전용 노드 문장 신설·평범한 next 기본 문장 유지. §8-⑥: 설계 초안 "대비 방법"은 기존 어휘와 통일해 "대체 경로"로 확정 |
| F5 run 소요 시간 표면화 | ✅머지 | #444 | web 1029(+9)·app unit·temp-db 게이트 전체(int+live e2e)·lint:copy | 계약 4면 동기(openapi Run 1곳=목록·상세 공유·api-surface §1·서버 2파일·web 타입)+README 패치 로그. **패치 번호 v2.35 사용**(설계 가칭 v2.36과 상이 — 최신 v2.34+1, F6은 web 전용이라 로그 불요). DDL·커서 무변경. 소요=종결 run만(경과 추정 금지), tabular-nums는 기존 관례 부재로 신규 인라인 적용 |
| F4 운영 알림 한국어 마감+enum 보정+벨 드롭다운 | ✅머지 | #446 | web 1036(벨 4→12케이스 재작성+대시보드 현지화)·app unit·로컬 게이트 전체 완주(int D6 가드+live e2e)·lint:copy | **README v2.36**(설계 순번 일치 — F5가 v2.35 사용). D5 확정 역어: lease→점유(기존 claimed="점유" 정합)·heartbeat→상태 신호(신규 무충돌)·circuit→회로 차단(CIRCUIT_LABELS "차단" 정합)·worker→실행기 — ⚠readiness 게이트 라벨 "브라우저 워커 이중화"(production-readiness-labels.ts)와 어휘 공존, 후속 통일 후보. readiness 증빙 라벨=web 게이트 라벨 5종 재사용. **가드 적발 추가 교정 2곳**(설계 실측 3곳 외 최소 적응): audit_verifier 권장 조치 영문 잔재·trigger_fire 원시 오류코드→카탈로그 userMessage+병기("예약 fire"→"예약 실행(fire)" 포함). D6 구현 노트: ERROR_CATALOG userMessage/operatorAction은 계약 소유 카피라 검사 전 제거(자리표시 "-"는 제외 — 1자 스트립이 "ops-pool"→"ops pool" 가짜 run 생성), 시드 display_name도 데이터 값으로 제거. OpsSignalPanel 로컬 소스 라벨 중복 제거(session_expiry 등 4종 "재처리 대기" 오라벨 결함 동반 수정). 비범위 기록: Layout 계정 팝오버의 usePopoverDismiss 개조(바깥 클릭 닫기 부재)=후속 후보 |
| F1 말로 고치기 계약+서버(prompt_redacted 영속+revise) | ✅머지 | #451 | app unit(신규 revise 유닛 20케이스)·temp-db 게이트 전체 완주(PG15 migration smoke+int 전체 `PASS: api-scenario-generations.int`+live e2e)·codegen typecheck/consistency/spectral·file-length·contract-lint | **README v2.37**(v2.35=F5, v2.36=F4 #446 머지 확인 후 확정). **스코프 조정: 설계 §1.3 web 항목(types-scenarios/client)은 F2 에이전트가 병행 구현 — 본 슬라이스는 web/ 무수정**(계약 문서+DDL+서버+app 테스트+README만). §8 선확인: ①start_url=params_context 보존(양 플래너가 params.start_url 스탬프·비민감 키라 redaction 생존), target=head 버전 IR에서 회수(run 경로 parseTarget(baseIr.target) 선례) → **원장 컬럼 확장 불필요 판정** ②version 경합=IFM-2 동형(ON CONFLICT DO NOTHING→rowCount≠1→SCENARIO_VERSION_CONFLICT, tx 내 head 재확인) ⑦evidence/planner/model=원 generation 값 그대로 승계 구현(예외 없음). 구현 노트: base_version 충돌의 HTTP status는 **412**(ERROR_CATALOG SCENARIO_VERSION_CONFLICT=412 — 설계 표기 409는 catalog 권위로 정정, PUT If-Match와 동일). prompt_redacted 채움은 boundary의 redactText 마스킹 직접 재사용(blocked 판정은 LLM 호출 시점 게이트웨이 소관 — 영속 경로 신규 거부 경로 없음, 과조임 회귀 방지). revise 라우트=신설 scenario-generation-revise.ts(scenario-generations.ts 500줄 상한 — 중복 signedCommandRefsFor 사본 제거로 467줄 확보, scenarios-support 공용 재사용). meta.version 스탬프 후 재컴파일(compiledAst.scenario_version 내장 — rollback 선례 동형). 기존 int "원장 행 전체 prompt 평문 부재" 단언은 신계약에 맞게 "draft_ir 평문 부재+prompt_redacted 보존·비밀 마스킹"으로 갱신 |
| F2 말로 고치기 UI+변경 표시 | ✅머지 | #449 | web 1056(+20)·typecheck·build·lint:copy | web/만 수정(계약·서버=F1 #451 소관, 병렬화 확정). API: reviseScenarioGeneration+ScenarioGenerationReviseRequest+목록 scenario_id 필터(fake 동기). 변경 표시: step-diff(node_id 기준, 키 정렬 직렬화로 순서 오인 방지)+StepCards changeMarks 슬롯(stepStates 동형)+빠진 단계 요약 행+전면 교체 안내("변경 없음" 추정 금지, §8-③). 표면 ①: ReviseControl 독립 컴포넌트+GenerationResult 최소 배선(성공=기존 selectGeneration 재사용으로 result 교체). 표면 ②: 설계 탭 DesignStepCards 아래 동일 컴포넌트(최신 generation 1건), §2.3 상태 전부 문장 표기(기록 없음+만들기 홈 딥링크/버전 충돌+재조회/scenario_not_persisted/instruction_required/instruction_too_long/prompt_too_long). base_version=화면 공유 scenario-detail 쿼리 version(If-Match 동형). **F1 머지 계약 대조 반영**: 버전 충돌=HTTP 412(설계 표기 409는 catalog 권위로 정정 — UI 분기는 처음부터 에러 코드 기준이라 동작 무변경, 주석·테스트만 412로 정정)+422 reason 2종(instruction_too_long/prompt_too_long) 문장 추가. 설계 갱신 기록: 클라이언트 합성 방식(상세 설계 §3.3 P0)은 도입하지 않음 — F1 revise가 §13 P1 이행으로 대체 |
| F3 만들기 홈 원패스 셸(세로 밀도 해소) | PR 생성(머지 게이트: F2 머지 후) | #450 | web 1042(+6 신규 create-onepass-shell)·typecheck·build·lint:copy·파일길이 게이트·app typecheck·console-e2e 12체크 green | useEasyGeneration=파생 phase만(IDLE/GENERATING/PREVIEW/TESTING; PRECHECK=IDLE 내 접힌 요약, 새로고침 IDLE 복귀=YAGNI 결정 기록). phase 매트릭스(§3.3): IDLE=준비단계/워크벤치/녹화 접힌 details(corridor blocked 자동 펼침·focus=test 딥링크 자동 펼침·chooser 녹화 선택 펼침), GENERATING=fieldset 잠금+진행 표시+나머지 숨김, PREVIEW=폼 접힌 요약("요청 고치기")+GenerationResult 주인공, TESTING=TestProgress 홈 내(RunScenarioButton onStarted, 화면 이동 없음). **e2e 밀도 가드: IDLE 실측 1,524px(전 ~3,300px 설계 실측) ×1.15≈1,753→임계 1,750px @1440×900** — 만들기 페르소나 전환(auth/readiness 효과 역할=UI 게이팅 우선 소스)+scenario-generations/capabilities 픽스처 추가(폴백 {items:[]}는 크래시). GenerationResult 수정=testAction 슬롯 1곳(F2 병행 충돌 최소화, 배선=GenerationTestAction). 폼 래퍼(GeneratorFormShell)=details+fieldset 요소 정체성 고정(phase 전환 리마운트로 하위 폼 상태 유실 방지 — 셸 v1 리마운트 결함을 테스트가 적발해 수정). 500줄 게이트로 useEvidencePolicySync 형제 분해(동작 불변, 생성기 480줄). 기존 테스트 갱신=보정 포커스 단언 2곳 waitFor(접힘 펼침 후 rAF 이연). 비고: 지시서의 e2e 8080 포트 주의는 stale(서버는 임시 포트 listen(0)) |
| P3 후속 정리 A/B/C | ✅머지 대기(본 PR) | — | web typecheck·test·build·lint:copy·app console-e2e(무관 확인) | F4·F5 행에서 비범위로 밀어 둔 후속 3건(독립 커밋 3개). **A** 계정 팝오버 usePopoverDismiss 개조: Escape만 처리→벨·생성 메뉴와 동일 규약(mousedown 바깥 닫기+Escape+트리거 포커스 복원), 화면/데스크톱 전환 닫기는 기존 setAccountOpen(false) 효과 보존, 트리거 aria-haspopup 추가+aria-controls 조건부. 테스트 2케이스(+2). **B** '워커'→'실행기' 표면 통일(선례 실행기(worker)): Security infra 목적 문구·readiness 게이트 라벨/문구 2곳·readiness 타일 detail·WorkerPoolPanel 4곳(제목만 '실행기 풀(worker pool)' 병기 1회, 나머지 병기 없음)·quickActions 키워드 '실행기' 추가('워커' 유지). meta.ts 'AI 실행기'(LLM 맥락)·주석·검색 키워드는 대상 아님. 테스트 단언 갱신(worker-pool 전량·security-section-nav heading 병기 반영). e2e는 worker 문자열 무단언(무관). **C** formatDurationMs(ms) 추출·export+formatRunDuration 재사용(동작 불변, 6케이스 그대로). 미통합 3곳+사유 명시: artifact-helpers(미디어 ms/s 소수 정밀도 손실), WebAttendedPanel(타임아웃 정책 'Nm' 표기 계약 상이), StepTrace DurationBar(막대 원시 ms). 경계 테스트(+4). |
| N1 생성 직후 미리보기 REDACTED 노출(3차 재감사 P2) | ✅머지 대기 | #453 | web 1077(+9: 신규 generation-result-preview 3·step-sentences +2·revise-control +3·벨 +1)·typecheck·build·lint:copy | 렌더 소스 전환: 저장 완료 결과는 저장본 IR(getScenario, scenario-detail 쿼리키 공유 — ReviseControl과 캐시 공유라 추가 요청 없음), draft_only는 현행 draft_ir 유지, 조회 실패=문장 표기. 토큰 안전망: step-sentences instruction 문장의 [REDACTED]/[REDACTED:...]→"보호된 요청 내용"(표시 계층만, TestProgress 등 draft_ir 잔여 소비처 커버). **diff 함정 처리**: FocusedScenarioStudio revise diff=저장본 v1 스냅샷 vs refetch된 저장본 v2(react-query data 객체 교체 대기) — 마스킹 차이 가짜 changed 제거. GenerationResult 내부 diff=응답끼리(양쪽 동일 redaction)라 정합 확인만. 한계 기록: 응답끼리 diff는 instruction-만 변경을 무표시(양쪽 마스킹 동일) — 저장본끼리 쓰는 스튜디오 탭은 감지 |
| N2 알림 소스 라벨 영문 잔존(3차 재감사 P2) | ✅머지 대기 | #453 | copy-gate PASS·벨/알림센터/스모크 108/108·web 전체 green | opsAlertSourceLabel(util/ops-alerts.ts 단일 출처) "Bot Pool"→"봇 풀"·"SCIM SecretRef"→"SCIM 서명 비밀"+필터 옵션 미러+"봇 풀 보기". F4 확정 조치 문구("봇 풀 용량, …" compute-governance.ts:309) 어휘 정합. 구 라벨 단언 테스트 전수 grep=0건(동반 수정 불요). 범위외 잔존 기록: WorkerPoolPanel.tsx:75 "Bot Pool 용량과 Graphile queue…"(알림 라벨 아닌 보안 패널 안내문) — 후속 후보 |
| N3 이름 미지정 생성 기본 이름 prompt-{hash}(3차 재감사 P3) | ✅머지 대기 | #453 | app 플래너 유닛 26/26(+8)·app typecheck·test:unit 전체 green | defaultGenerationScenarioName: redaction 통과본(F1 prompt_redacted 동일 경로 — redactGenerationPrompt) 요약, 토큰 제거·공백 정규화·앞 24자+" 자동화", 비면 "말로 만든 자동화 {yyyy-MM-dd}". 지점=scenario-generation-planner.ts:201(구 `prompt-${hash12}`). 충돌=기존 scenario_name_in_use 거절 관례 유지(동일 프롬프트는 종전 해시도 동일 충돌 — 정책 무변경, 단 앞 24자 겹치는 상이 프롬프트 간 신규 충돌 가능성은 동 관례로 시끄럽게 거절됨을 기록). **계약 판정: api-surface.md·openapi.yaml에 기본 이름 형식 미문서화 → 구현 세부, 계약 무변경(README 로그 불요)**. 대상 외: failed-ledger "failed-prompt-{hash}"(scenario_id NULL 실패 원장 내부 IR — 목록 비노출)·llm_v1 meta.name(LLM 출력 소유) |
| N4 중복 표기 2건(3차 재감사 P3) | ✅머지 대기 | #453 | 벨 12/12·revise-control 15/15(신규 4케이스 포함)·web 전체 green | ①벨 팝오버 항목 부제=opsAlertSourceLabel이 제목에 포함되면 생략(예: "재처리 대기 DLQ" vs "재처리 대기"), 미포함 시 유지. ②StepCards 흐름 전용 노드(terminal 등)=문장==흐름 라벨이라 배지 중복("완료합니다"×2)→문장과 동일한 배지 억제, 동작 있는 노드 배지는 유지(과억제 금지 단언) |
| 잔존 후속 2건: WorkerPoolPanel 영문 카피(F4 어휘)·revise diff 저장본 승격 | ✅머지 대기 | #456 | web 1079(+2: instruction-만 변경 표시·draft_only 회귀)·typecheck·build·lint:copy | ①N2 범위외 잔존(#453 기록) 해소: 보안 패널 안내문을 F4 확정 어휘("봇 풀 용량, 만료된 점유(lease), 실행기 상태 신호(heartbeat)…")로 교정+동일 파일 잔존 일괄(Drain→비우기(drain)·Priority→우선순위·queued→대기·stale→신호 없음, WORKER_POOL_KEYS 원문 유지) ②GenerationResult(저장 완료) revise diff 를 저장본끼리(v1 vs v2) 비교로 승격 — 응답끼리 비교는 양쪽 instruction 마스킹으로 instruction-만 수정이 무표시(#453 한계). 스냅샷·v2 도착 대기 로직은 use-saved-revise-diff 공용 훅 추출(스튜디오 설계 탭과 공유), draft_only 는 응답끼리 유지 |
| AI 거버넌스 게이트 발견성 3건(준비도 실측에서 발견) | ✅머지 대기 | 본 PR | web typecheck·test 1086(+3)·build·lint:copy·file-length·app console-e2e 12체크 green·**실 서버 라이브 검증**(dev:serve + 실 Chrome) | 발단: `GET /v1/ops/production-readiness` 실측(게이트 13=blocked 5·deferred 6·pass 2) 중 ai_governance_runtime 이 콘솔로 닫히는지 확인하다 발견. 게이트는 **닫힌다**(정책 저장→요구 subject_ref 증빙 3건→pass 실측). 단 3개 결함이 그 경로를 가렸다. ①**조용한 실패**: 준비도는 (evidence_type + subject_ref) **정확 일치**로만 요구를 충족(evaluateEvidenceRequirements)하는데, 요구 subject 는 테넌트 `gateway_policies.model` 파생(`model:claude-haiku` 등)이고 기록기 기본값은 `model:codex-prod-primary` → 기본값대로 기록하면 201 로 남지만 게이트는 열린 채. 수정=요구 증빙을 기록기에 버튼으로 제시(클릭=종류+대상 참조 주입)+불일치 시 "준비 상태는 바뀌지 않습니다" 경고. ②**오류 문구**: 증빙 거부(422 `IR_SCHEMA_INVALID` + details.reason)가 콘솔에 "시나리오 정의 오류."로 표시(코드 라벨만 사용) → errorLabel 이 details.reason 매핑을 코드 라벨보다 **우선**(미매핑은 코드 라벨 폴백, 조용한 공백 금지). 감사 추적 ID 획득 경로도 문장화(감사 이력 > '이 번호로 조회'). ③**미노출**: 요구 subject_ref 는 서버 signals.ai_governance.requirements 에 이미 있었으나 web 타입·화면에 없었음 → 타입 추가 + 준비도 패널에 미충족 요구만 노출(AiGovernanceRequirementList). 숨은 전제 기록: status=valid 증빙은 **테넌트 감사로그에 실재하는** audit_correlation_id 필수(assertAuditCorrelationExists) → 감사 이벤트 0건인 신규 테넌트는 증빙 기록 불가(영구 교착 아님 — 역할 배정·run 우선순위·시나리오 승격 등 appendGovernanceAudit 경로가 채움). 부수 확인: audit_chain_evidence 도 콘솔 '감사 검증 실행'으로 blocked→pass(차단 5→4). |
