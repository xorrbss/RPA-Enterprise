# Claude Code 로컬 런타임/설정
.claude/

# 로컬 시크릿/환경(실 API 키 등) — 절대 커밋 금지.
.env
.env.*

# OS cruft
.DS_Store
Thumbs.db
__pycache__/
*.pyc

# 스냅샷 아카이브 (CLAUDE.md: 별도 요청 없으면 무시)
files_v1.2_patched.zip

# codegen 의존 설치물(생성물 아님). package.json/package-lock로 재현.
codegen/node_modules/

# app(D2+ 런타임) 의존 설치물. package.json/package-lock로 재현.
app/node_modules/

# D3 PoC/executor 의존 설치물. package.json/package-lock로 재현.
app/poc/**/node_modules/

# PoC 런타임 로그(일회성).
app/**/poc.log
app/exec.log

# 캡처 PoC 산출물 — 라이브 세션 쿠키/시크릿 포함, 절대 커밋 금지.
app/poc/d3-stagehand/capture-reviews-output.json
